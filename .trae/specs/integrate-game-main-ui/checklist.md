# 集成游戏主界面验收清单

> **当前归类**：进行中任务文档（待实现）  
> **使用边界**：勾选结果表示本次实现的验收通过，不代表后续演进中不会再次引入同类问题。  
> **现行真源**：实际代码、`docs/development/quality-gates.md`

## 功能验收

- [ ] 访问 `/` 默认展示“游戏主界面”（非英雄列表）。
- [ ] 主界面可进入：英雄、主城/城建、采集、任务、抽卡/开箱、战斗、攻城、联盟等入口（按本次集成清单）。
- [ ] 从 City/Gathering/LootBox/TowerDefense/Cooking/Siege 等玩法页退出后回到 `/` 主界面。
- [ ] 联盟页可通过路由访问并正常渲染（至少能展示 AllianceDashboard 的基础界面）。
- [ ] 主界面与功能页均可正常使用钱包连接组件（连接/断开不报错）。

## 兼容与回归

- [ ] 现有玩法路由（如 `/city`、`/gathering`、`/gacha`、`/battle` 等）仍可直接访问。
- [ ] 旧的“退出回首页”逻辑不需要逐个玩法改造即可回到主界面（依赖 `/` 的语义变化）。

## 工程质量门禁

- [ ] `npm run build` 成功。
- [ ] `npm run test` 成功（如无测试变更，可仅确保无新增失败）。
- [ ] `npm run lint` 成功或无新增告警。
- [ ] `npm run lint:style` 成功或无新增告警。

